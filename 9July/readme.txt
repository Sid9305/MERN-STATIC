React  - JSX
JS + HTML 

<!-- public : static files -->
<!-- src : dynamic files -->

react 
JSX
folder/file structure
khudke component
reusability component
return single / multiple element
css in react 
external css : not in same file

      {/* every element has a unique id */}
      {/* multiple element can have same class */}
      {/* multiple classes can belong to same element */}
      {/* <p className="thispara class1 class2 class3">This is paragraph part 1</p> */}
      {/* ctrl + / or ctrl + ?  : for comments */}

css: tailwindcss 
bootstrap : templates site

      {/* style = {{js inline css}} */}
      {/* camel Casing */}
      {/* firstword small , next word, first letter capital */}


NOTE:
node_modules
npm install : woh wapas aajayenge
package.json : dependencies
component name : first letter capital
React components are regular JavaScript functions, but their names must start with a capital letter or they won’t work!


// export default : woh naam nhi dekhta 

export - import components 

props : parameters

State : component ki memory